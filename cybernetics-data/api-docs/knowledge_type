# Table: knowledge_type

## API Operations

### 1. Read Records (GET)
```bash
curl -X GET "https://cybernetics.avarile.com/api/table/tblWcq6Kof1AFHvbC5e/record?fieldKeyType=name" \
  -H "Authorization: Bearer <YOUR_API_TOKEN>"
```

#### Pagination
Use `skip` and `take` parameters:
- `take`: Number of records to return (default: 100, max: 1000)
- `skip`: Number of records to skip

```bash
# Get 20 records, starting from the 41st record (page 3)
curl "https://cybernetics.avarile.com/api/table/tblWcq6Kof1AFHvbC5e/record?take=20&skip=40&fieldKeyType=name" \
  -H "Authorization: Bearer <YOUR_API_TOKEN>"
```

#### Filtering
Use the `filter` parameter with a JSON object.

**⚠️ Important: The `fieldId` in filter/orderBy MUST use the actual field ID (e.g., "fldXXXX"), not the field name.**

```bash
# Filter records - use field ID from the Fields section above
curl "https://cybernetics.avarile.com/api/table/tblWcq6Kof1AFHvbC5e/record?fieldKeyType=name" \
  --data-urlencode 'filter={"conjunction":"and","filterSet":[{"fieldId":"fldXXXXXXX","operator":"is","value":"Active"}]}' \
  -H "Authorization: Bearer <YOUR_API_TOKEN>"
```

**Filter Operators**:
- Text: `is`, `isNot`, `contains`, `doesNotContain`, `isEmpty`, `isNotEmpty`
- Number: `is`, `isNot`, `isGreater`, `isLess`, `isGreaterEqual`, `isLessEqual`
- Date: `is`, `isBefore`, `isAfter`, `isWithin`

#### Sorting
Use the `orderBy` parameter.

**⚠️ Important: The `fieldId` in orderBy MUST use the actual field ID (e.g., "fldXXXX"), not the field name.**

```bash
# Sort by a field - use field ID from the Fields section above
curl "https://cybernetics.avarile.com/api/table/tblWcq6Kof1AFHvbC5e/record?fieldKeyType=name" \
  --data-urlencode 'orderBy=[{"fieldId":"fldXXXXXXX","order":"desc"}]' \
  -H "Authorization: Bearer <YOUR_API_TOKEN>"
```

#### Field Selection (Projection)
Use the `projection` parameter to return only specific fields:
```bash
# Only return "Name" and "Email" fields
curl "https://cybernetics.avarile.com/api/table/tblWcq6Kof1AFHvbC5e/record?fieldKeyType=name&projection=Name&projection=Email" \
  -H "Authorization: Bearer <YOUR_API_TOKEN>"
```

#### Searching
Use the `search` parameter:
```bash
# Search for "john" in all fields
curl "https://cybernetics.avarile.com/api/table/tblWcq6Kof1AFHvbC5e/record?search=john&fieldKeyType=name" \
  -H "Authorization: Bearer <YOUR_API_TOKEN>"
```

### 2. Create Record (POST)
```bash
curl -X POST "https://cybernetics.avarile.com/api/table/tblWcq6Kof1AFHvbC5e/record" \
  -H "Authorization: Bearer <YOUR_API_TOKEN>" \            {
    "fieldKeyType": "name",
    "record": {
      "fields": {
        // Include only fields you want to update
      }                                                  [id: fldsnpIqBqoYZJxZwI5] (Long text / Rich text)
  - "created_at" [id: fldAPzS5RXnhHUrVlmu] (Created time (auto-generated)) [READ-ONLY]
  - "updated_at" [id: fld1TMYMRUBblTyTIuU] (Last modified time (auto-generated)) [READ-ONLY]
  - "deleted_at" [id: fldMcIaV4FSsuV9OG9J] (Date/Time)                       HvbC5e (relationship: manyOne))

---

## Example Record
                                                                         ],
        "credentials": "Sample text",
        "child_types": [
          {
            "id": "reccVgbTtiSGacQURZG",                                                  lationship: oneMany]
  - "parent_type" references knowledge_type (tblWcq6Kof1AFHvbC5e) — provide a single record reference like `{ "id": "recXXX" }` [relationship: manyOne]
                                                                                                       n table each field points to
- Response format: `{ "records": [{ fields: { ... } }] }`

---

## Notes for AI
- Fields marked [PRIMARY] are the main identifier field
- Fields marked [READ-ONLY] are computed and cannot be directly modified
- Use `fieldKeyType=name` to reference fields by their display name in request/response body
- **Important**: `filter` and `orderBy` parameters MUST use field IDs (the [id: fldXXX] shown above), not field names
- Dates should be in ISO 8601 format (e.g., "2024-01-15T10:30:00Z")
- For select fields, use the exact option names listed above
- For link fields, see the **Example Record** section above for the exact reference shape and the foreig
            "title": "deployment - docker-compose"
          },
          {
            "id": "recwKFT8vCKe1bm9LYn",
            "title": "deployment - k8s"
          }
        ],
        "parent_type": {
          "id": "reccVgbTtiSGacQURZG",
          "title": "deployment - docker-compose"
        }
      }
    }
  ]
}
```

### Link / Reference Fields
  - "knowledges" references knowledges (tblVTWb1kxXSFPBq4Fq) — provide an array of record references like `{ "id": "recXXX" }` [relationship: oneMany]
  - "child_types" references knowledge_type (tblWcq6Kof1AFHvbC5e) — provide an array of record references like `{ "id": "recXXX" }` [re
A sample `POST` body for creating a record. Link fields use real record IDs fetched from the referenced tables where available; `recXXXXXXXXXXXXXX` is a placeholder when the referenced table is empty.

```json
{
  "fieldKeyType": "name",
  "records": [
    {
      "fields": {
        "title": "Sample text",
        "context": "A longer sample description spanning a sentence or two.",
        "deleted_at": "2026-08-29T00:59:42.028Z",
        "is_active": true,
        "knowledges": [
          {
            "id": "recjjkJqhOlcT1LGR4j",
            "title": "Plane - docker-compose"
          },
          {
            "id": "recrHVpZPhA85MMsXhU",
            "title": "Onyx - docker-compose"
          }
 
  - "is_active" [id: fldte086UL30roxKKAl] (Checkbox (true/false))
  - "id" [id: fldD6MxhSrpatAbHd0k] (Auto-incrementing number) [READ-ONLY]
  - "knowledges" [id: fldtBWHVokGwcAywjtc] (Link to table tblVTWb1kxXSFPBq4Fq (relationship: oneMany))
  - "credentials" [id: fld1s2dEom17aZIu4nx] (Single line text)
  - "child_types" [id: fldehPY6CjjdpeK3H6j] (Link to table tblWcq6Kof1AFHvbC5e (relationship: oneMany))
  - "parent_type" [id: fld924GlXY0tL5um2wk] (Link to table tblWcq6Kof1AF
    }
  }'
```

### 4. Delete Record (DELETE)
```bash
curl -X DELETE "https://cybernetics.avarile.com/api/table/tblWcq6Kof1AFHvbC5e/record/{recordId}" \
  -H "Authorization: Bearer <YOUR_API_TOKEN>"
```

---

## API Configuration
- **Base URL**: https://cybernetics.avarile.com
- **Table ID**: tblWcq6Kof1AFHvbC5e
- **API Token**: <YOUR_API_TOKEN>
- **Endpoint**: `https://cybernetics.avarile.com/api/table/tblWcq6Kof1AFHvbC5e/record`

## Authentication
All requests require the `Authorization` header:
```
Authorization: Bearer <YOUR_API_TOKEN>
```

---

## Fields
  - "title" [id: fldvL1LqKmEAfNKVBCO] (Single line text) [PRIMARY]
  - "context"           [118;1:3u
  -H "Content-Type: application/json" \
  -d '{
    "fieldKeyType": "name",
    "records": [
      {
        "fields": {
          // Editable fields: "title", "context", "deleted_at", "is_active", "knowledges", "credentials", "child_types", "parent_type"
        }
      }
    ]
  }'
```

### 3. Update Record (PATCH)
```bash
curl -X PATCH "https://cybernetics.avarile.com/api/table/tblWcq6Kof1AFHvbC5e/record/{recordId}" \
  -H "Authorization: Bearer <YOUR_API_TOKEN>" \
  -H "Content-Type: application/json" \
  -d '
