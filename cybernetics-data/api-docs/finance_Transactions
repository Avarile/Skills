# Table: finance_Transactions

## API Operations

### 1. Read Records (GET)
```bash
curl -X GET "https://cybernetics.avarile.com/api/table/tblylfLkXFkYH2qB0ZQ/record?fieldKeyType=name" \
  -H "Authorization: Bearer <YOUR_API_TOKEN>"
```

#### Pagination
Use `skip` and `take` parameters:
- `take`: Number of records to return (default: 100, max: 1000)
- `skip`: Number of records to skip

```bash
# Get 20 records, starting from the 41st record (page 3)
curl "https://cybernetics.avarile.com/api/table/tblylfLkXFkYH2qB0ZQ/record?take=20&skip=40&fieldKeyType=name" \
  -H "Authorization: Bearer <YOUR_API_TOKEN>"
```

#### Filtering
Use the `filter` parameter with a JSON object.

**⚠️ Important: The `fieldId` in filter/orderBy MUST use the actual field ID (e.g., "fldXXXX"), not the field name.**

```bash
# Filter records - use field ID from the Fields section above
curl "https://cybernetics.avarile.com/api/table/tblylfLkXFkYH2qB0ZQ/record?fieldKeyType=name" \
  --data-urlencode 'filter={"conjunction":"and","filterSet":[{"fieldId":"fldXXXXXXX","operator":"is","value":"Active"}]}' \
  -H "Authorization: Bearer <YOUR_API_TOKEN>"
```

**Filter Operators**:
- Text: `is`, `isNot`, `contains`, `doesNotContain`, `isEmpty`, `isNotEmpty`
- Number: `is`, `isNot`, `isGreater`, `isLess`, `isGreaterEqual`, `isLessEqual`
- Date: `is`, `isBefore`, `isAfter`, `isWithin`

#### Sorting
Use the `orderBy` parameter.

**⚠️ Important: The `fieldId` in orderBy MUST use the actual field ID (e.g., "fldXXXX"), not the field name.**

```bash
# Sort by a field - use field ID from the Fields section above
curl "https://cybernetics.avarile.com/api/table/tblylfLkXFkYH2qB0ZQ/record?fieldKeyType=name" \
  --data-urlencode 'orderBy=[{"fieldId":"fldXXXXXXX","order":"desc"}]' \
  -H "Authorization: Bearer <YOUR_API_TOKEN>"
```

#### Field Selection (Projection)
Use the `projection` parameter to return only specific fields:
```bash
# Only return "Name" and "Email" fields
curl "https://cybernetics.avarile.com/api/table/tblylfLkXFkYH2qB0ZQ/record?fieldKeyType=name&projection=Name&projection=Email" \
  -H "Authorization: Bearer <YOUR_API_TOKEN>"
```

#### Searching
Use the `search` parameter:
```bash
# Search for "john" in all fields
curl "https://cybernetics.avarile.com/api/table/tblylfLkXFkYH2qB0ZQ/record?search=john&fieldKeyType=name" \
  -H "Authorization: Bearer <YOUR_API_TOKEN>"
```
                                                                      <YOUR_API_TOKEN>" \
  -H "Content-Type: application/json" \
  -d '{
    "fieldKeyType": "name",
    "record": {
      "fields": {                                                      [118;1:3uion" [id: fldRZcBYKr6w2GWw5lm] (Single line text) [PRIMARY]
  - "Date" [id: fldZIgdLJ79xoDQK5u4] (Date/Time)                dlU2BpJBZFcNcFhom] (Link to table tblp2Jvb2g90PN3fX4d (relationship: manyOne))
  - "Payee" [id: fld8WNu5jDIr55RNkSu] (Link to table tblpyC3PfoG0DN8dQ2t (relation is empty.

```json
{
  "fieldKeyType": "name",
  "records": [
    {                                                                  fUcLFYh",
          "title": "RSSVM23 Pty Ltd"
        },
        "Tags": [
          {                                                                 tegories (tblp2Jvb2g90PN3fX4d) — provide a single record reference like `{ "id": "recXXX" }` [relationship: manyOne]
  - "Payee" references finance_Payees (tblpyC3PfoG0DN8dQ2t) — provide a single recplay name in request/response body
- **Important**: `filter` and `orderBy` parameters MUST use field IDs (the [id: fldXXX] shown above), not field names
- Dates should be in ISO 8601 format (e.g., "2024-01-15T10:30:00Z")
- For select fields, use the exact option names listed above
- For link fields, see the **Example Record** section above for the exact reference shape and the foreign table each field points to
- Response format: `{ "records": [{ fields: { ... } }] }`
ord reference like `{ "id": "recXXX" }` [relationship: manyOne]
  - "Tags" references finance_Tags (tblhOQKXpSzr0Ezh8rR) — provide an array of record references like `{ "id": "recXXX" }` [relationship: manyMany]
  - "Budget" references finance_Budgets (tblDk3iXFz5YiYUkwz6) — provide a single record reference like `{ "id": "recXXX" }` [relationship: manyOne]

---

## Notes for AI
- Fields marked [PRIMARY] are the main identifier field
- Fields marked [READ-ONLY] are computed and cannot be directly modified
- Use `fieldKeyType=name` to reference fields by their dis
            "id": "rec2pMOdsQaLYSS9PJ7",
            "title": "Recurring / Subscription"
          },
          {
            "id": "recatWYRBqPaNSNCc5s",
            "title": "Tax Deductible"
          }
        ],
        "Budget": {
          "id": "recXXXXXXXXXXXXXX"
        }
      }
    }
  ]
}
```

### Link / Reference Fields
  - "Account" references finance_Accounts (tblJLklwpGwbrfDlwIz) — provide a single record reference like `{ "id": "recXXX" }` [relationship: manyOne]
  - "Transfer Account" references finance_Accounts (tblJLklwpGwbrfDlwIz) — provide a single record reference like `{ "id": "recXXX" }` [relationship: manyOne]
  - "Category" references finance_Ca
      "fields": {
        "Description": "Sample text",
        "Date": "2026-08-29T00:57:40.819Z",
        "Type": "Income",
        "Amount": 42,
        "Recurring": true,
        "Recurring Frequency": "Weekly",
        "Notes": "A longer sample description spanning a sentence or two.",
        "Account": {
          "id": "recXqZCzGDIz4aRaygC",
          "title": "ANZ Access Advantage (2992-86276)"
        },
        "Transfer Account": {
          "id": "recXqZCzGDIz4aRaygC",
          "title": "ANZ Access Advantage (2992-86276)"
        },
        "Category": {
          "id": "recnc9bBSjv58bsInmi",
          "title": "Food"
        },
        "Payee": {
          "id": "recULZYeYcy6ship: manyOne))
  - "Tags" [id: fldXRiV3NxrspHNLQFO] (Link to table tblhOQKXpSzr0Ezh8rR (relationship: manyMany))
  - "Budget" [id: fldVYJYqgs9p2GQSf9a] (Link to table tblDk3iXFz5YiYUkwz6 (relationship: manyOne))
  - "Scope" [id: fldV9EFPhFI8W0OOMbF] (Single select (options: Personal, Business)) [READ-ONLY]
  - "Signed Amount" [id: flddkw1CxatR4zrvXd0] (Computed formula field) [READ-ONLY]

---

## Example Record

A sample `POST` body for creating a record. Link fields use real record IDs fetched from the referenced tables where available; `recXXXXXXXXXXXXXX` is a placeholder when the referenced table
  - "Type" [id: fldIqRcX7gZ1zn9Kmz0] (Single select (options: Income, Expense, Transfer))
  - "Amount" [id: flde1ox2n7Q30UUP938] (Number)
  - "Recurring" [id: fldAiqG7oB0DCPeDL7K] (Checkbox (true/false))
  - "Recurring Frequency" [id: fldTMF1faN48Lrd3vS4] (Single select (options: Weekly, Monthly, Yearly))
  - "Receipt" [id: fldlgu9WLxc0FznWjO5] (File attachments)
  - "Notes" [id: fldW2RsBfTxd362FjfO] (Long text / Rich text)
  - "Account" [id: fldcg0wB2iVE8DZCVHm] (Link to table tblJLklwpGwbrfDlwIz (relationship: manyOne))
  - "Transfer Account" [id: fldIeT7aNuRjEAVlyqL] (Link to table tblJLklwpGwbrfDlwIz (relationship: manyOne))
  - "Category" [id: fl
        // Include only fields you want to update
      }
    }
  }'
```

### 4. Delete Record (DELETE)
```bash
curl -X DELETE "https://cybernetics.avarile.com/api/table/tblylfLkXFkYH2qB0ZQ/record/{recordId}" \
  -H "Authorization: Bearer <YOUR_API_TOKEN>"
```

---

## API Configuration
- **Base URL**: https://cybernetics.avarile.com
- **Table ID**: tblylfLkXFkYH2qB0ZQ
- **API Token**: <YOUR_API_TOKEN>
- **Endpoint**: `https://cybernetics.avarile.com/api/table/tblylfLkXFkYH2qB0ZQ/record`

## Authentication
All requests require the `Authorization` header:
```
Authorization: Bearer <YOUR_API_TOKEN>
```

---

## Fields
  - "Descript
### 2. Create Record (POST)
```bash
curl -X POST "https://cybernetics.avarile.com/api/table/tblylfLkXFkYH2qB0ZQ/record" \
  -H "Authorization: Bearer <YOUR_API_TOKEN>" \
  -H "Content-Type: application/json" \
  -d '{
    "fieldKeyType": "name",
    "records": [
      {
        "fields": {
          // Editable fields: "Description", "Date", "Type", "Amount", "Recurring", "Recurring Frequency", "Receipt", "Notes", "Account", "Transfer Account", "Category", "Payee", "Tags", "Budget"
        }
      }
    ]
  }'
```

### 3. Update Record (PATCH)
```bash
curl -X PATCH "https://cybernetics.avarile.com/api/table/tblylfLkXFkYH2qB0ZQ/record/{recordId}" \
  -H "Authorization: Bearer
