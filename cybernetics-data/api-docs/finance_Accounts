
# Table: finance_Accounts

## API Operations

### 1. Read Records (GET)
```bash
curl -X GET "https://cybernetics.avarile.com/api/table/tblJLklwpGwbrfDlwIz/record?fieldKeyType=name" \
  -H "Authorization: Bearer <YOUR_API_TOKEN>"
```

#### Pagination
Use `skip` and `take` parameters:
- `take`: Number of records to return (default: 100, max: 1000)
- `skip`: Number of records to skip

```bash
# Get 20 records, starting from the 41st record (page 3)
curl "https://cybernetics.avarile.com/api/table/tblJLklwpGwbrfDlwIz/record?take=20&skip=40&fieldKeyType=name" \
  -H "Authorization: Bearer <YOUR_API_TOKEN>"
```

#### Filtering
Use the `filter` parameter with a JSON object.

**⚠️ Important: The `fieldId` in filter/orderBy MUST use the actual field ID (e.g., "fldXXXX"), not the field name.**

```bash
# Filter records - use field ID from the Fields section above
curl "https://cybernetics.avarile.com/api/table/tblJLklwpGwbrfDlwIz/record?fieldKeyType=name" \
  --data-urlencode 'filter={"conjunction":"and","filterSet":[{"fieldId":"fldXXXXXXX","operator":"is","value":"Active"}]}' \
  -H "Authorization: Bearer <YOUR_API_TOKEN>"
```

**Filter Operators**:
- Text: `is`, `isNot`, `contains`, `doesNotContain`, `isEmpty`, `isNotEmpty`
- Number: `is`, `isNot`, `isGreater`, `isLess`, `isGreaterEqual`, `isLessEqual`
- Date: `is`, `isBefore`, `isAfter`, `isWithin`

#### Sorting
Use the `orderBy` parameter.

**⚠️ Important: The `fieldId` in orderBy MUST use the actual field ID (e.g., "fldXXXX"), not the field name.**

```bash
# Sort by a field - use field ID from the Fields section above
curl "https://cybernetics.avarile.com/api/table/tblJLklwpGwbrfDlwIz/record?fieldKeyType=name" \
  --data-urlencode 'orderBy=[{"fieldId":"fldXXXXXXX","order":"desc"}]' \
  -H "Authorization: Bearer <YOUR_API_TOKEN>"
```

#### Field Selection (Projection)
Use the `projection` parameter to return only specific fields:
```bash
# Only return "Name" and "Email" fields
curl "https://cybernetics.avarile.com/api/table/tblJLklwpGwbrfDlwIz/record?fieldKeyType=name&projection=Name&projection=Email" \
  -H "Authorization: Bearer <YOUR_API_TOKEN>"
```

#### Searching
Use the `search` parameter:
```bash
# Search for "john" in all fields
curl "https://cybernetics.avarile.com/api/table/tblJLklwpGwbrfDlwIz/record?search=john&fieldKeyType=name" \
  -H "Authorization: Bearer <YOUR_API_TOKEN>"
```

### 2. Create Record (POST)
```bash
curl -X POST "https://cybernetics.avarile.com/api/table/tblJLklwpGwbrfDlwIz/record" \
  -H "Authorization: Bearer <YOUR_API_TOKEN>" \
  -H "Content-Type: application/json" \
  -d '{
    "fieldKeyType": "name",
    "records": [
      {
        "fields": {
          // Editable fields: "Name", "Scope", "Type", "Institution", "Opening Balance", "Opening Balance Date", "Active", "finance_Transactions", "finance_Transactions (linked)"
        }
      }
    ]
  }'
```

### 3. Update Record (PATCH)
```bash
curl -X PATCH "https://cybernetics.avarile.com/api/table/tblJLklwpGwbrfDlwIz/record/{recordId}" \
  -H "Authorization: Bearer <YOUR_API_TOKEN>" \
  -H "Content-Type: application/json" \
  -d '{
    "fieldKeyType": "name",
    "record": {
      "fields": {
        // Include only fields you want to update
      }
    }
  }'
```

### 4. Delete Record (DELETE)
```bash
curl -X DELETE "https://cybernetics.avarile.com/api/table/tblJLklwpGwbrfDlwIz/record/{recordId}" \
  -H "Authorization: Bearer <YOUR_API_TOKEN>"
```

---

## API Configuration
- **Base URL**: https://cybernetics.avarile.com
- **Table ID**: tblJLklwpGwbrfDlwIz
- **API Token**: <YOUR_API_TOKEN>
- **Endpoint**: `https://cybernetics.avarile.com/api/table/tblJLklwpGwbrfDlwIz/record`

## Authentication
All requests require the `Authorization` header:
```
Authorization: Bearer <YOUR_API_TOKEN>
```

---

## Fields
  - "Name" [id: fldSzvd0NAglB9qhsp1] (Single line text) [PRIMARY]
  - "Scope" [id: fldZFxk5O8qT6AF6QV2] (Single select (options: Personal, Business))
  - "Type" [id: fldaZgFxywk8cZT9l8V] (Single select (options: Checking, Savings, Credit Card, Cash, Investment, Mortgage))
  - "Institution" [id: fldEHOdmX1nYcAaaybH] (Single line text)
  - "Opening Balance" [id: fldiOOafvV1UPDJtV1f] (Number)
  - "Opening Balance Date" [id: fldBjBPMxjYpGxs0yJ5] (Date/Time)
  - "Active" [id: fldRyzp4jLRtGqUd6gu] (Checkbox (true/false))
  - "finance_Transactions" [id: fld5aM7gN7NhK7AYuVM] (Link to table tblylfLkXFkYH2qB0ZQ (relationship: oneMany))
  - "finance_Transactions (linked)" [id: flddwaQQ81OY23P2Jbt] (Link to table tblylfLkXFkYH2qB0ZQ (relationship: oneMany))
  - "Own Txn Total" [id: fldxGuTHGj0kFXhVEQ0] (Rollup (aggregation from linked records)) [READ-ONLY]
  - "Incoming Transfer Total" [id: fldShPCh4zNp2NRgBYo] (Rollup (aggregation from linked records)) [READ-ONLY]
  - "Net Activity" [id: fld04GdZkCiVlkrABed] (Computed formula field) [READ-ONLY]
  - "Current Balance" [id: fldSTUAw8v0UMcTivzf] (Computed formula field) [READ-ONLY]

---

## Example Record

A sample `POST` body for creating a record. Link fields use real record IDs fetched from the referenced tables where available; `recXXXXXXXXXXXXXX` is a placeholder when the referenced table is empty.

```json
{
  "fieldKeyType": "name",
  "records": [
    {
      "fields": {
        "Name": "Sample text",
        "Scope": "Personal",
        "Type": "Checking",
        "Institution": "Sample text",
        "Opening Balance": 42,
        "Opening Balance Date": "2026-08-29T00:56:57.614Z",
        "Active": true,
        "finance_Transactions": [
          {
            "id": "recFZhvA8xluWtFFvO7",
            "title": "RSSVM23 PTY LTD POINT COOK"
          },
          {
            "id": "recTFeW3c1XBI9j8D4x",
            "title": "RSSVM23 PTY LTD POINT COOK"
          }
        ],
        "finance_Transactions (linked)": [
          {
            "id": "recFZhvA8xluWtFFvO7",
            "title": "RSSVM23 PTY LTD POINT COOK"
          },
          {
            "id": "recTFeW3c1XBI9j8D4x",
            "title": "RSSVM23 PTY LTD POINT COOK"
          }
        ]
      }
    }
  ]
}
```

### Link / Reference Fields
  - "finance_Transactions" references finance_Transactions (tblylfLkXFkYH2qB0ZQ) — provide an array of record references like `{ "id": "recXXX" }` [relationship: oneMany]
  - "finance_Transactions (linked)" references finance_Transactions (tblylfLkXFkYH2qB0ZQ) — provide an array of record references like `{ "id": "recXXX" }` [relationship: oneMany]

---

## Notes for AI
- Fields marked [PRIMARY] are the main identifier field
- Fields marked [READ-ONLY] are computed and cannot be directly modified
- Use `fieldKeyType=name` to reference fields by their display name in request/response body
- **Important**: `filter` and `orderBy` parameters MUST use field IDs (the [id: fldXXX] shown above), not field names
- Dates should be in ISO 8601 format (e.g., "2024-01-15T10:30:00Z")
- For select fields, use the exact option names listed above
- For link fields, see the **Example Record** section above for the exact reference shape and the foreign table each field points to
- Response format: `{ "records": [{ fields: { ... } }] }`
