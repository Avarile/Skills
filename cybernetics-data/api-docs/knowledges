# Table: knowledges

## API Operations

### 1. Read Records (GET)
```bash
curl -X GET "https://cybernetics.avarile.com/api/table/tblVTWb1kxXSFPBq4Fq/record?fieldKeyType=name" \
  -H "Authorization: Bearer <YOUR_API_TOKEN>"
```

#### Pagination
Use `skip` and `take` parameters:
- `take`: Number of records to return (default: 100, max: 1000)
- `skip`: Number of records to skip

```bash
# Get 20 records, starting from the 41st record (page 3)
curl "https://cybernetics.avarile.com/api/table/tblVTWb1kxXSFPBq4Fq/record?take=20&skip=40&fieldKeyType=name" \
  -H "Authorization: Bearer <YOUR_API_TOKEN>"
```

#### Filtering
Use the `filter` parameter with a JSON object.

**⚠️ Important: The `fieldId` in filter/orderBy MUST use the actual field ID (e.g., "fldXXXX"), not the field name.**

```bash    " \
  -d '{
    "fieldKeyType": "name",
    "record": {
      "fields": {
        // Include only fields you want to update
      }
    }
  }'
```

### 4. Delete Record (DELETE)
```bash
curl -X DELETE "h "context" [id: fld5tr2rH8oJXLrjUo9] (Long text / Rich text)
  - "created_at" [id: fldRs8i67CkuzpDyubP] (Created time (auto-generated)) [READ-ONLY]
  - "updated_at" [id: fldCpv0ts2jxxgQPg7n] (Last modified time (auto-generated)) [READ-ONLY]
  - "deleted_at" [id: fldNS9SNNWG07NnBZhE] (Date/Time)
  - "is_active" [id: fldZKMuaBBPd6tIzSG3] (Checkbox (true/false))
  - "id" [id: fldNxPGGDuchpHCvz0U] (Auto-incrementing number) [READ-ONLY]
  - "knowledge_type" [id: fldAEK8ULw9urxE0qiF] (Link to table tblWcq6Kof1AFHvbC5e (relationship: manyOne))
  - "knowledge_parent" [id: fldzUUVy3q1vSWURhZD] (Link to table tblVTWb1kxXSFPBq4Fq (relationship: manyOne))
  - "knowledges" [id: fldKzPJEFtElf4liepG] (Link to table tblVTWb1kxXSFPBq4Fq (relationship: oneMany))
  - "related_ttps://cybernetics.avarile.com/api/table/tblVTWb1kxXSFPBq4Fq/record/{recordId}" \
  -H "Authorization: Bearer <YOUR_API_TOKEN>"
```

---                   knowledge" [id: fldz7U2RsCfm0j1RZOs] (Link to table tblVTWb1kxXSFPBq4Fq (relationship: manyMany))

---

## Example Record

A sample `POST` body for creating a record. Link fields use real record IDs fetched from the referenced tables where available; `recXXXXXXXXXXXXXX` is a placeholder when the referenced table is empty.

```json
{
  "fieldKeyType": "name",
  "records": [
    {
      "fields": {          qhOlcT1LGR4j",
          "title": "Plane - docker-compose"
        },
        "knowledges": [
          {
            "id": "recjjkJqhOlcT1LGR4j",
            "title": "Plane - docker-compose"
          },
          {
            "id": "recrHVpZPhA85MMsXhU",                    X" }` [relationship: manyOne]
  - "knowledge_parent" references knowledges (tblVTWb1kxXSFPBq4Fq) — provide a single record reference like `{ "id": "recXXX" }` [relationship: manyOne]
  - "knowledges" references knowledges (tblVTWb1kxXSFPBq4Fq) — provide an array of record references like `{ "id": "recXXX" }` [relationship: oneMany]
  - "related_knowledge" references knowledges (tblVTWb1kxXSFPBq4Fq) — provide an array of record references like `{ "id": "recXXX" }` [relationship: manyMany]

---

## Notes for AI                                                                      orderBy` parameters MUST use field IDs (the [id: fldXXX] shown above), not field names
- Dates should be in ISO 8601 format (e.g., "2024-01-15T10:30:00Z")
- For select fields, use the exact option names listed above
- For link fields, see the **Example Record** section above for the exact reference shape and the foreign table each field points to
- Response format: `{ "records": [{ fields: { ... } }] }`

- Fields marked [PRIMARY] are the main identifier field
- Fields marked [READ-ONLY] are computed and cannot be directly modified
- Use `fieldKeyType=name` to reference fields by their display name in request/response body
- **Important**: `filter` and `
            "title": "Onyx - docker-compose"
          }
        ],
        "related_knowledge": [
          {
            "id": "recjjkJqhOlcT1LGR4j",
            "title": "Plane - docker-compose"
          },
          {
            "id": "recrHVpZPhA85MMsXhU",
            "title": "Onyx - docker-compose"
          }
        ]
      }
    }
  ]
}
```

### Link / Reference Fields
  - "knowledge_type" references knowledge_type (tblWcq6Kof1AFHvbC5e) — provide a single record reference like `{ "id": "recXX
        "title": "Sample text",
        "context": "A longer sample description spanning a sentence or two.",
        "deleted_at": "2026-08-29T00:59:09.458Z",
        "is_active": true,
        "knowledge_type": {
          "id": "reccVgbTtiSGacQURZG",
          "title": "deployment - docker-compose"
        },
        "knowledge_parent": {
          "id": "recjjkJ

## API Configuration
- **Base URL**: https://cybernetics.avarile.com
- **Table ID**: tblVTWb1kxXSFPBq4Fq
- **API Token**: <YOUR_API_TOKEN>
- **Endpoint**: `https://cybernetics.avarile.com/api/table/tblVTWb1kxXSFPBq4Fq/record`

## Authentication
All requests require the `Authorization` header:
```
Authorization: Bearer <YOUR_API_TOKEN>
```

---

## Fields
  - "title" [id: fldROFj15OlD8COVxX0] (Single line text) [PRIMARY]
  -
# Filter records - use field ID from the Fields section above
curl "https://cybernetics.avarile.com/api/table/tblVTWb1kxXSFPBq4Fq/record?fieldKeyType=name" \
  --data-urlencode 'filter={"conjunction":"and","filterSet":[{"fieldId":"fldXXXXXXX","operator":"is","value":"Active"}]}' \
  -H "Authorization: Bearer <YOUR_API_TOKEN>"
```

**Filter Operators**:
- Text: `is`, `isNot`, `contains`, `doesNotContain`, `isEmpty`, `isNotEmpty`
- Number: `is`, `isNot`, `isGreater`, `isLess`, `isGreaterEqual`, `isLessEqual`
- Date: `is`, `isBefore`, `isAfter`, `isWithin`

#### Sorting
Use the `orderBy` parameter.

**⚠️ Important: The `fieldId` in orderBy MUST use the actual field ID (e.g., "fldXXXX"), not the field name.**

```bash
# Sort by a field - use field ID from the Fields section above
curl "https://cybernetics.avarile.com/api/table/tblVTWb1kxXSFPBq4Fq/record?fieldKeyType=name" \
  --data-urlencode 'orderBy=[{"fieldId":"fldXXXXXXX","order":"desc"}]' \
  -H "Authorization: Bearer <YOUR_API_TOKEN>"
```

#### Field Selection (Projection)
Use the `projection` parameter to return only specific fields:
```bash
# Only return "Name" and "Email" fields
curl "https://cybernetics.avarile.com/api/table/tblVTWb1kxXSFPBq4Fq/record?fieldKeyType=name&projection=Name&projection=Email" \
  -H "Authorization: Bearer <YOUR_API_TOKEN>"
```

#### Searching
Use the `search` parameter:
```bash
# Search for "john" in all fields
curl "https://cybernetics.avarile.com/api/table/tblVTWb1kxXSFPBq4Fq/record?search=john&fieldKeyType=name" \
  -H "Authorization: Bearer <YOUR_API_TOKEN>"
```

### 2. Create Record (POST)
```bash
curl -X POST "https://cybernetics.avarile.com/api/table/tblVTWb1kxXSFPBq4Fq/record" \
  -H "Authorization: Bearer <YOUR_API_TOKEN>" \
  -H "Content-Type: application/json" \
  -d '{
    "fieldKeyType": "name",
    "records": [
      {
        "fields": {
          // Editable fields: "title", "context", "deleted_at", "is_active", "knowledge_type", "knowledge_parent", "knowledges", "related_knowledge"
        }
      }
    ]
  }'
```

### 3. Update Record (PATCH)
```bash
curl -X PATCH "https://cybernetics.avarile.com/api/table/tblVTWb1kxXSFPBq4Fq/record/{recordId}" \
  -H "Authorization: Bearer <YOUR_API_TOKEN>" \
  -H "Content-Type: application/json
