# Table: finance_Categories

## API Operations

### 1. Read Records (GET)
```bash
curl -X GET "https://cybernetics.avarile.com/api/table/tblp2Jvb2g90PN3fX4d/record?fieldKeyType=name" \
  -H "Authorization: Bearer <YOUR_API_TOKEN>"
```

#### Pagination
Use `skip` and `take` parameters:
- `take`: Number of records to return (default: 100, max: 1000)
- `skip`: Number of records to skip

```bash
# Get 20 records, starting from the 41st record (page 3)
curl "https://cybernetics.avarile.com/api/table/tblp2Jvb2g90PN3fX4d/record?take=20&skip=40&fieldKeyType=name" \
  -H "Authorization: Bearer <YOUR_API_TOKEN>"
```

#### Filtering
Use the `filter` parameter with a JSON object.

**⚠️ Important: The `fieldId` in filter/orderBy MUST use the actual field ID (e.g., "fldXXXX"), not the field name.**

```bash
# Filter records - use field ID from the Fields section above
curl "https://cybernetics.avarile.com/api/table/tblp2Jvb2g90PN3fX4d/record?fieldKeyType=name" \
  --data-urlencode 'filter={"conjunction":"and","filterSet":[{"fieldId":"fldXXXXXXX","operator":"is","value":"Active"}]}' \
  -H "Authorization: Bearer <YOUR_API_TOKEN>"
```

**Filter Operators**:
- Text: `is`, `isNot`, `contains`, `doesNotContain`, `isEmpty`, `isNotEmpty`
- Number: `is`, `isNot`, `isGreater`, `isLess`, `isGreaterEqual`, `isLessEqual`
- Date: `is`, `isBefore`, `isAfter`, `isWithin`

#### Sorting
Use the `orderBy` parameter.

**⚠️ Important: The `fieldId` in orderBy MUST use the actual field ID (e.g., "fldXXXX"), not the field name.**

```bash
# Sort by a field - use field ID from the Fields section above
curl "https://cybernetics.avarile.com/api/table/tblp2Jvb2g90PN3fX4d/record?fieldKeyType=name" \
  --data-urlencode 'orderBy=[{"fieldId":"fldXXXXXXX","order":"desc"}]' \
  -H "Authorization: Bearer <YOUR_API_TOKEN>"
```

#### Field Selection (Projection)
Use the `projection` parameter to return only specific fields:
```bash
# Only return "Name" and "Email" fields
curl "https://cybernetics.avarile.com/api/table/tblp2Jvb2g90PN3fX4d/record?fieldKeyType=name&projection=Name&projection=Email" \
  -H "Authorization: Bearer <YOUR_API_TOKEN>"
```

#### Searching
Use the `search` parameter:
```bash
# Search for "john" in all fields
curl "https://cybernetics.avarile.com/api/table/tblp2Jvb2g90PN3fX4d/record?search=john&fieldKeyType=name" \
  -H "Authorization: Bearer <YOUR_API_TOKEN>"
```

### 2. Create Record (POST)
```bash
curl -X POST "https://cybernetics.avarile.com/api/table/tblp2Jvb2g90PN3fX4d/record" \
  -H "Authorization: Bearer <YOUR_API_TOKEN>" \
  -H "Content-Type: application/json" \
  -d '{
    "fieldKeyType": "name",
    "records": [
      {
        "fields": {
          // Editable fields: "Name", "Type", "Scope", "Parent Category", "finance_Categories", "finance_Payees", "finance_Budgets", "finance_Transactions"
        }
      }
    ]
  }'
```

### 3. Update Record (PATCH)
```bash
curl -X PATCH "https://cybernetics.avarile.com/api/table/tblp2Jvb2g90PN3fX4d/record/{recordId}" \
  -H "Authorization: Bearer <YOUR_API_TOKEN>" \
  -H "Content-Type: a[118;1:3upplication/json" \
  -d '{
    "fieldKeyType": "name",
    "record": {
      "fields": {
        // Include only fi [PRIMARY]
  - "Type" [id: fldBazKuxdsorHnlCmZ]))
  - "finance_Transactions" [id: fld2hxfGgtrPZk5h8cW] (Link to table tblylfLkXFkYH2qB0ZQ (relationship: oneMany))

---

## Example Record                c40aPr5LW86CbBl6y",
            "title": "Dining & Takeaway"
          }
        ],
        "finance_Payees": [
          {
            "id": "recULZYeYcy6fUcLFYh",
            "title": "RSSVM23 Ptygory" references finance_Categories (tblp2Jvb2g90PN3fX4d) — provide a single record reference like `{ "id": "recXXX" }` [relationship: manyOne]
  - "finance_Categories" references finance_Categories (tblp2Jvb2g90PN3fX4d) — provide an array of record references like `{ "id": "recXXX" }` [relationship: oneMany]
  - "finance_Payees" references finance_Payees (tblpyC3PfoG0DN8dQ2t) — provide an array of record references like `{ "id": "recXXX" }` [relationship: oneMany]
  - "finance_Budgets" references finance_Budgets (tblDk3iXFz5YiYUkwz6) — provide an array of record references like `{ "id": "recXXX" }` [relationship: oneMany]
  - "finance_Transactions" references finance_Transactions (tblylfLkXFkYH2qB0ZQ) — provide an array of record references like `{ "id": "r Ltd"
          },
          {
            "id": "reckMf3EzHrbRecXXX" }` [relationship: oneMany]

---

## Notes for AI
- Fields marked [PRIMARY] are the main identifier field
- Fields marked [READ-ONLY] are computed and cannot be directly modified
- Use `fieldKeyType=name` to reference fields by their display name in request/response body
- **Important**: `filter` and `orderBy` parameters MUST use field IDs (the [id: fldXXX] shown above), not field names
- Dates should be in ISO 8601 format (e.g., "2024-01-15T10:30:00Z")
- For select fields, use the exact option names listed above
- For link fields, see the **Example Record** section above for the exact reference shape and the foreign table each field points to
- Response format: `{ "records": [{ fields: { ... } }] }`
xJ6ISa",
            "title": "Coles"
          }
        ],
        "finance_Budgets": [
          {
            "id": "recXXXXXXXXXXXXXX"
          }
        ],
        "finance_Transactions": [
          {
            "id": "recFZhvA8xluWtFFvO7",
            "title": "RSSVM23 PTY LTD POINT COOK"
          },
          {
            "id": "recTFeW3c1XBI9j8D4x",
            "title": "RSSVM23 PTY LTD POINT COOK"
          }
        ]
      }
    }
  ]
}
```

### Link / Reference Fields
  - "Parent Cate

A sample `POST` body for creating a record. Link fields use real record IDs fetched from the referenced tables where available; `recXXXXXXXXXXXXXX` is a placeholder when the referenced table is empty.

```json
{
  "fieldKeyType": "name",
  "records": [
    {
      "fields": {
        "Name": "Sample text",
        "Type": "Income",
        "Scope": "Personal",
        "Parent Category": {
          "id": "recnc9bBSjv58bsInmi",
          "title": "Food"
        },
        "finance_Categories": [
          {
            "id": "recnc9bBSjv58bsInmi",
            "title": "Food"
          },
          {
            "id": "re (Single select (options: Income, Expense))
  - "Scope" [id: flddiHU2kAnTeZG49P4] (Single select (options: Personal, Business, Shared))
  - "Parent Category" [id: fldmknXtvhmpIUmjTcP] (Link to table tblp2Jvb2g90PN3fX4d (relationship: manyOne))
  - "finance_Categories" [id: fldQADwmtwhv2Qe0UCa] (Link to table tblp2Jvb2g90PN3fX4d (relationship: oneMany))
  - "Parent Name" [id: fldYtNHk7iAhQlQHQvd] (Single line text) [READ-ONLY]
  - "Full Path" [id: fldZZlGOiOHRb84DjDV] (Computed formula field) [READ-ONLY]
  - "finance_Payees" [id: fldPKGGlI8LBZ9GOO5F] (Link to table tblpyC3PfoG0DN8dQ2t (relationship: oneMany))
  - "finance_Budgets" [id: fld3qDsS1na7qJVvPZ1] (Link to table tblDk3iXFz5YiYUkwz6 (relationship: oneManyelds you want to update
      }
    }
  }'
```

### 4. Delete Record (DELETE)
```bash
curl -X DELETE "https://cybernetics.avarile.com/api/table/tblp2Jvb2g90PN3fX4d/record/{recordId}" \
  -H "Authorization: Bearer <YOUR_API_TOKEN>"
```

---

## API Configuration
- **Base URL**: https://cybernetics.avarile.com
- **Table ID**: tblp2Jvb2g90PN3fX4d
- **API Token**: <YOUR_API_TOKEN>
- **Endpoint**: `https://cybernetics.avarile.com/api/table/tblp2Jvb2g90PN3fX4d/record`

## Authentication
All requests require the `Authorization` header:
```
Authorization: Bearer <YOUR_API_TOKEN>
```

---

## Fields
  - "Name" [id: flddvHdoGLLLyFYTXlM] (Single line text)
