# Table: finance_Budgets

## API Operations

### 1. Read Records (GET)
```bash
curl -X GET "https://cybernetics.avarile.com/api/table/tblDk3iXFz5YiYUkwz6/record?fieldKeyType=name" \
  -H "Authorization: Bearer <YOUR_API_TOKEN>"
```

#### Pagination
Use `skip` and `take` parameters:
- `take`: Number of records to return (default: 100, max: 1000)
- `skip`: Number of records to skip

```bash
# Get 20 records, starting from the 41st record (page 3)
curl "https://cybernetics.avarile.com/api/table/tblDk3iXFz5YiYUkwz6/record?take=20&skip=40&fieldKeyType=name" \
  -H "Authorization: Bearer <YOUR_API_TOKEN>"
```

#### Filtering
Use the `filter` parameter with a JSON object.

**⚠️ Important: The `fieldId` in filter/orderBy MUST use the actual field ID (e.g., "fldXXXX"), not the field name.**

```bash
# Filter records - use field ID from the Fields section above
curl "https://cybernetics.avarile.com/api/table/tblDk3iXFz5YiYUkwz6/record?fieldKeyType=name" \
  --data-urlencode 'filter={"conjunction":"and","filterSet":[{"fieldId":"fldXXXXXXX","operator":"is","value":"Active"}]}' \
  -H "Authorization: Bearer <YOUR_API_TOKEN>"
```

**Filter Operators**:
- Text: `is`, `isNot`, `contains`, `doesNotContain`, `isEmpty`, `isNotEmpty`
- Number: `is`, `isNot`, `isGreater`, `isLess`, `isGreaterEqual`, `isLessEqual`
- Date: `is`, `isBefore`, `isAfter`, `isWithin`

#### Sorting
Use the `orderBy` parameter.

**⚠️ Important: The `fieldId` in orderBy MUST use the actual field ID (e.g., "fldXXXX"), not the field name.**

```bash
# Sort by a field - use field ID from the Fields section above
curl "https://cybernetics.avarile.com/api/table/tblDk3iXFz5YiYUkwz6/record?fieldKeyType=name" \
  --data-urlencode 'orderBy=[{"fieldId":"fldXXXXXXX","order":"desc"}]' \
  -H "Authorization: Bearer <YOUR_API_TOKEN>"
```

#### Field Selection (Projection)
Use the `projection` parameter to return only specific fields:
```bash
# Only return "Name" and "Email" fields
curl "https://cybernetics.avarile.com/api/table/tblDk3iXFz5YiYUkwz6/record?fieldKeyType=name&projection=Name&projection=Email" \
  -H "Authorization: Bearer <YOUR_API_TOKEN>"
```

#### Searching
Use the `search` parameter:
```bash
# Search for "john" in all fields
curl "https://cybernetics.avarile.com/api/table/tblDk3iXFz5YiYUkwz6/record?search=john&fieldKeyType=name" \
  -H "Authorization: Bearer <YOUR_API_TOKEN>"
```

### 2. Create Record (POST)
```bash
curl -X POST "https://cybernetics.avarile.com/api/table/tblDk3iXFz5YiYUkwz6/record" \
  -H "Authorization: Bearer <YOUR_API_TOKEN>" \
  -H "Content-Type: application/json" \
  -d '{                                                    [118;1:3u
    "fieldKeyType": "name",
    "records": [
      {                                           : {
      "fields": {
        // Include only fields you want to update
      }
    }
  }'
```

### 4. Delete Record (DELETE)
```bash                                          ategory" [id: fldfCZXAsjS3nEM0MGW] (Link to table tblp2Jvb2g90PN3fX4d (relationship: manyOne))
  - "Scope" [id: fldWtNgqZiz6JcSAUkB] (Single select (options: Personal, Business, Shared)) [READ-ONLY]
  - "finance_Transactions" [id: fld0MY2qyEpHKu2xqqP] (Link to table tblylfLkXFkYH2qB0ZQ (relationship: oneMany))
  - "Actual Spent" [id: fldCEyMLCffN6AU15n9] (Rollup (aggregation from linked records)) [READ-ONLY]
  - "Variance" [id: fldBbkdGPNoAwaD1nMc] (Computed formula field) [READ-ONLY]
  - "% Used" [id: fldYGBsE6yNmObjpM3B] (Computed erenced table is empty.

```json
{
  "fieldKeyType": "name",
  "records": [                              ld points to
- Response format: `{ "records": [{ fields: { ... } }] }`
ransactions" references finance_Transactions (tblylfLkXFkYH2qB0ZQ) — provide an array of record references like `{ "id": "recXXX" }` [relationship: oneMany]

---

## Notes for AI
- Fields marked [PRIMARY] are the main identifier field
- Fields marked [READ-ONLY] are computed and cannot be directly modified
- Use `fieldKeyType=name` to reference fields by their display name in request/response body
- **Important**: `filter` and `orderBy` parameters MUST use field IDs (the [id: fldXXX] shown above), not field names
- Dates should be in ISO 8601 format (e.g., "2024-01-15T10:30:00Z")
- For select fields, use the exact option names listed above
- For link fields, see the **Example Record** section above for the exact reference shape and the foreign table each fie
    {
      "fields": {
        "Month": "2026-08-29T00:57:21.155Z",
        "Planned Amount": 42,
        "Category": {
          "id": "recnc9bBSjv58bsInmi",
          "title": "Food"
        },
        "finance_Transactions": [
          {
            "id": "recFZhvA8xluWtFFvO7",
            "title": "RSSVM23 PTY LTD POINT COOK"
          },
          {
            "id": "recTFeW3c1XBI9j8D4x",
            "title": "RSSVM23 PTY LTD POINT COOK"
          }
        ]
      }
    }
  ]
}
```

### Link / Reference Fields
  - "Category" references finance_Categories (tblp2Jvb2g90PN3fX4d) — provide a single record reference like `{ "id": "recXXX" }` [relationship: manyOne]
  - "finance_Tformula field) [READ-ONLY]

---

## Example Record

A sample `POST` body for creating a record. Link fields use real record IDs fetched from the referenced tables where available; `recXXXXXXXXXXXXXX` is a placeholder when the ref
curl -X DELETE "https://cybernetics.avarile.com/api/table/tblDk3iXFz5YiYUkwz6/record/{recordId}" \
  -H "Authorization: Bearer <YOUR_API_TOKEN>"
```

---

## API Configuration
- **Base URL**: https://cybernetics.avarile.com
- **Table ID**: tblDk3iXFz5YiYUkwz6
- **API Token**: <YOUR_API_TOKEN>
- **Endpoint**: `https://cybernetics.avarile.com/api/table/tblDk3iXFz5YiYUkwz6/record`

## Authentication
All requests require the `Authorization` header:
```
Authorization: Bearer <YOUR_API_TOKEN>
```

---

## Fields
  - "Month" [id: fldzRSk36rF2iLqoc1A] (Date/Time) [PRIMARY]
  - "Planned Amount" [id: fldJXYFV5eXNnALW40c] (Number)
  - "C
        "fields": {
          // Editable fields: "Month", "Planned Amount", "Category", "finance_Transactions"
        }
      }
    ]
  }'
```

### 3. Update Record (PATCH)
```bash
curl -X PATCH "https://cybernetics.avarile.com/api/table/tblDk3iXFz5YiYUkwz6/record/{recordId}" \
  -H "Authorization: Bearer <YOUR_API_TOKEN>" \
  -H "Content-Type: application/json" \
  -d '{
    "fieldKeyType": "name",
    "record"
